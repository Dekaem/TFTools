{% extends '/admin/admin_base.html.twig' %}

{% block title %}Ajouter un champion{% endblock %}

{% block body %}
    <div class="container">
        <h1>Ajouter un champion</h1>
        {{  form_start(form) }}

        <div class="form-row">
            {{  form_row(form.nom) }}
        </div>

        <div class="form-row">
            {{  form_row(form.illustration) }}
        </div>

        <div class="form-row">
            {{  form_row(form.origines) }}
        </div>

        {{  form_end(form) }}
    </div>


{% endblock %}
