{% extends '/admin/admin_base.html.twig' %}

{% block title %}Ajouter une origine{% endblock %}

{% block body %}
    <div class="container">
        <h1>Ajouter une origine</h1>
        {{  form_start(form) }}

        <div class="form-row">
            {{  form_row(form.nom) }}
        </div>

        <div class="form-row">
            {{  form_row(form.description) }}
        </div>

        <div class="form-row">
            {{  form_row(form.placeMoyenne) }}
        </div>

        {{  form_end(form) }}
    </div>


{% endblock %}
